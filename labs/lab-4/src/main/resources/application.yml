spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  application:
    name: lab-4
  docker:
    compose:
      file: labs/lab-4/compose.yaml

# Book Metadata API (OpenLibrary)
book:
  metadata:
    api:
      url: https://openlibrary.org
      timeout: 5
server:
  port: 8888
